{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FileWatcher watchconfig.json",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "settings": {
      "$ref": "#/definitions/settings"
    },
    "env": {
      "$ref": "#/definitions/env"
    },
    "hooks": {
      "$ref": "#/definitions/hooks"
    }
  },
  "definitions": {
    "env": {
      "type": "object",
      "description": "Template variables. Each \"{{key}}\" in a hook's command, source, copyTo, or location is replaced with its mapped value. Keys prefixed with \"path:\" are smart-joined so separators never collide.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "nonWhitespaceString": {
      "type": "string",
      "pattern": "\\S"
    },
    "settings": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "debounceMs": {
          "type": "integer",
          "minimum": 0,
          "default": 1000
        }
      }
    },
    "hooks": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "onStartup": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/startupHook"
          },
          "default": []
        },
        "onUpdate": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/updateHook"
          },
          "default": []
        }
      }
    },
    "startupHook": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "command"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "command": {
          "$ref": "#/definitions/nonWhitespaceString"
        },
        "location": {
          "type": "string"
        },
        "logLevel": {
          "type": "string",
          "default": "Info"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        }
      }
    },
    "updateHook": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source"
      ],
      "anyOf": [
        {
          "required": [
            "command"
          ]
        },
        {
          "required": [
            "copyTo"
          ]
        }
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "source": {
          "$ref": "#/definitions/nonWhitespaceString"
        },
        "copyTo": {
          "$ref": "#/definitions/nonWhitespaceString"
        },
        "command": {
          "$ref": "#/definitions/nonWhitespaceString"
        },
        "location": {
          "type": "string"
        },
        "logLevel": {
          "type": "string",
          "default": "Info"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        }
      }
    }
  }
}
